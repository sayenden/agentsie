/* MailerLite Embed Loader
 * Replace the contents below with your official MailerLite embed code.
 * Example: paste the script tag MailerLite gives you, and write into the container with id="ml-embed".
 *
 * For example (pseudo):
 * (function(w,d,src){
 *   var s=d.createElement('script'); s.src=src; s.async=true;
 *   d.getElementById('ml-embed').appendChild(s);
 * })(window, document, 'https://assets.mailerlite.com/js/universal.js');
 */
// Temporary fallback (no-op) so the page renders without errors
console.debug('MailerLite embed placeholder loaded. Replace assets/mailer-embed.js with your real embed.');
